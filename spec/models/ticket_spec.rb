require 'rails_helper'

RSpec.describe Ticket, type: :model do


#  let(:closed_ticket) { build(:closed_ticket) }
#  let(:open_ticket) { build(:open_ticket) }

#  describe 'relationships' do
#    it { should belong_to(:region) }
#  end

#  describe "#open"
#    it "retrieves only non_closed tickets without an organization" do
#      open_tickets = Tickets.open
#      expect(open_tickets).to include(open_ticket)
#      expecct(open_tickets).not_to include(closed_ticket)
#    end
#  end

end
